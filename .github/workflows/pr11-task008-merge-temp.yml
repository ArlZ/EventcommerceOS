name: PR11 Task 008 merge temporary

on:
  push:
    branches: [codex/task-009-event-close]

permissions:
  contents: write

jobs:
  merge-base:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/task-009-event-close
          fetch-depth: 0
      - name: Merge final Task 008 base
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin codex/task-008-command-centre
          set +e
          git merge --no-ff --no-edit origin/codex/task-008-command-centre
          status=$?
          set -e
          if [ "$status" -ne 0 ]; then
            echo "Merge conflicts:"
            git diff --name-only --diff-filter=U
            git status --short
            exit "$status"
          fi
      - name: Self-remove and push
        run: |
          git rm .github/workflows/pr11-task008-merge-temp.yml
          git commit -m "Remove temporary PR11 base merge workflow"
          git push origin HEAD:codex/task-009-event-close
