name: Task006ProviderConflictRepair
on:
  push:
    branches: [task/006-payment-domain-mpesa]
    paths:
      - '.github/workflows/task006-provider-conflict-repair.yml'
permissions:
  contents: write
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: task/006-payment-domain-mpesa
      - uses: pnpm/action-setup@v4
        with:
          version: 10.12.1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: python scripts/task006_provider_conflict_repair.py
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec prettier --write apps/cloud-api/src/payments/payment.service.ts
      - run: |
          rm scripts/task006_provider_conflict_repair.py
          rm .github/workflows/task006-provider-conflict-repair.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/cloud-api/src/payments/payment.service.ts scripts/task006_provider_conflict_repair.py .github/workflows/task006-provider-conflict-repair.yml
          git commit -m "Quarantine conflicting provider payment identifiers"
          git push origin HEAD:task/006-payment-domain-mpesa
