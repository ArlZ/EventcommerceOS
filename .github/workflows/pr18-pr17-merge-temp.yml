name: PR18 final PR17 merge temporary

on:
  push:
    branches: [security/abuse-controls]

permissions:
  contents: write

jobs:
  merge-base:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: security/abuse-controls
          fetch-depth: 0
      - name: Merge final PR17 base
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin security/human-auth-rbac
          set +e
          git merge --no-ff --no-edit origin/security/human-auth-rbac
          status=$?
          set -e
          if [ "$status" -ne 0 ]; then
            echo "Merge conflicts:"
            git diff --name-only --diff-filter=U
            git status --short
            exit "$status"
          fi
      - name: Self-remove and push
        run: |
          git rm .github/workflows/pr18-pr17-merge-temp.yml
          git commit -m "Remove temporary PR18 final PR17 merge workflow"
          git push origin HEAD:security/abuse-controls
