name: PR15 final PR14 merge temporary

on:
  push:
    branches: [security/pos-edge-trust]

permissions:
  contents: write

jobs:
  merge-base:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: security/pos-edge-trust
          fetch-depth: 0
      - name: Merge final PR14 base
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin security/edge-cloud-trust
          set +e
          git merge --no-ff --no-edit origin/security/edge-cloud-trust
          status=$?
          set -e
          if [ "$status" -ne 0 ]; then
            echo "Merge conflicts:"
            git diff --name-only --diff-filter=U
            git status --short
            exit "$status"
          fi
      - name: Self-remove and push
        run: |
          git rm .github/workflows/pr15-pr14-merge-temp.yml
          git commit -m "Remove temporary PR15 final PR14 merge workflow"
          git push origin HEAD:security/pos-edge-trust
