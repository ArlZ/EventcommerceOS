# Hostinger managed Node.js Web App: Cloud API
# Add these in hPanel deployment environment variables. Never commit real secrets.
NODE_ENV=production
PORT=3000

# Public HTTPS origin of the separately deployed Event Control Web app.
CONTROL_WEB_ORIGIN=https://control.pilot.example.com

# External PostgreSQL connection string. For Supabase, use a PostgreSQL connection
# string appropriate for a persistent server workload and require TLS.
DATABASE_URL=postgresql://replace-user:replace-password@replace-host:5432/replace-database?sslmode=require
DATABASE_CONNECTION_TIMEOUT_MS=5000

# Pilot abuse-protection mode. Validate Hostinger proxy behavior before field use.
ABUSE_DEPLOYMENT_MODE=single_instance_pilot
ABUSE_UPSTREAM_CONFIRMED=false
TRUST_PROXY_HOPS=1

# Pilot 1 remains Safaricom sandbox-only.
MPESA_BASE_URL=https://sandbox.safaricom.co.ke
MPESA_CALLBACK_URL=https://api.pilot.example.com/payments/providers/mpesa/callback
MPESA_TRANSACTION_TYPE=CustomerPayBillOnline
MPESA_TIMEOUT_MS=10000
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_BUSINESS_SHORT_CODE=
MPESA_PASSKEY=

# Optional release identity for health output. Set this to the deployed full Git SHA
# when promoting a controlled candidate. Hostinger does not populate it automatically.
RELEASE_COMMIT=
